# Dummy credentials so the context (and the ProjectApiRoot bean) builds offline in tests.
# No live call is made: the worked slice uses a mocked repository and the catalog stub
# throws before any SDK call.
ctp.project-key=test-project
ctp.client-id=test-client-id
ctp.client-secret=test-client-secret
ctp.auth-url=https://auth.europe-west1.gcp.commercetools.com
ctp.api-url=https://api.europe-west1.gcp.commercetools.com
ctp.store-key=

# Track progress in-memory during tests (no commercetools writes).
training.participant-id=test-participant
training.participant-name=Test Participant
training.progress-store=memory
