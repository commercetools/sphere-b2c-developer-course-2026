# Commerce Canvas holds NO commercetools credentials — it only talks to the BFF training API.

# BFF origin proxied at /api during dev (either the Java or the NestJS BFF).
VITE_BFF_ORIGIN=http://localhost:8081

# Where "View in Storefront" deep-links to.
VITE_STOREFRONT_URL=http://localhost:3000
